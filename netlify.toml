# Configuration Netlify pour MIDEESSI

[build]
  publish = "dist"
  command = "npm run build"

# Variables d'environnement pour le build
[build.environment]
  NODE_VERSION = "18"

# ============================================================
# CACHE BUSTING - Pour éviter les problèmes de cache sur mobile
# ============================================================

# HTML - Toujours vérifier la version la plus récente
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# index.html - Toujours à jour
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Assets avec hash (JS, CSS) - Cache long terme sûr
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# API/Supabase - Never cache
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# ============================================================
# IMPORTANT : Les redirections avec conditions User-Agent
# Pour les crawlers de réseaux sociaux, proxy vers le serveur SEO Render
# L'URL reste mideessi.com mais le contenu vient de Render
# ============================================================

# Facebook
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["facebookexternalhit*", "Facebot*"]}

# Twitter
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["Twitterbot*"]}

# LinkedIn
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["LinkedInBot*"]}

# WhatsApp
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["WhatsApp*"]}

# Slack
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["Slackbot*"]}

# Discord
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["Discordbot*"]}

# Pinterest
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["Pinterest*"]}

# Google
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["Googlebot*"]}

# Bing
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["bingbot*"]}

# Telegram
[[redirects]]
  from = "/blog/*"
  to = "https://mideessi-seo.onrender.com/blog/:splat"
  status = 200
  force = true
  conditions = {User-Agent = ["TelegramBot*"]}

# Pour TOUS les autres visiteurs (navigateurs normaux)
# Servir le SPA React normalement
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers de sécurité
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache pour les assets statiques
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.jpg"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.png"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.webp"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"